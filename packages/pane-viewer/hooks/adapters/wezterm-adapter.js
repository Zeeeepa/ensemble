/**
 * WezTerm adapter - re-export from shared package
 * @deprecated Import from '@fortium/ensemble-multiplexer-adapters' instead
 */
const { WeztermAdapter } = require('@fortium/ensemble-multiplexer-adapters');
module.exports = { WeztermAdapter };
